<%= link_to "Add new books" , new_book_path%>
<% @books.reverse_each do |f| %>
<div>
  <div class="background-tile">
  <p>Title : <%= f.title %></p>
  <p>Uploaded By : <%= f.user_name %></p>
    	<%= link_to "Read More", book_path(f), :class => 'btn btn-default' %>
</div>
	<hr>
</div>
<% end %>
